const MAJOR_VERSION = 0;
const MINOR_VERSION = 1;
const PATCH_VERSION = 0;
//This should get updated upon every API update
export const LOX_VERSION = `v${MAJOR_VERSION}.${MINOR_VERSION}.${PATCH_VERSION}`;
